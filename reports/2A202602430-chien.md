# Individual contribution report

## Thông tin

- Họ và tên: Phùng Trọng Chiến
- Mã học viên: 2A202602430
- Nhóm: T040-Skynet
- Repository/branch: `K4-L3B-RAG-Pipeline-T040-Skynet` / `chien`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| RRF reranking | Nhận kết quả dense và BM25 từ Track B; tính điểm theo `sum(1/(k+rank))`; loại ID trùng, sắp xếp giảm dần và gắn nhãn `hybrid`. | `src/task7_reranking.py` | Done |
| Mock cho RRF | Tạo hai danh sách dense/BM25 có phần tử trùng để kiểm tra điểm, thứ tự và khả năng loại trùng. | `data/task7_mock_input.json` | Done |
| PageIndex fallback | Cài đặt upload lazy, cache `source → doc_id`, timeout, bắt lỗi, chuyển kết quả về schema chung; trả `[]` an toàn khi thiếu API key. | `src/task8_pageindex_vectorless.py` | Done |
| Retrieval pipeline | Nối dense + BM25 + RRF; dùng điểm cosine gốc để quyết định fallback; nếu PageIndex rỗng hoặc lỗi thì trả kết quả hiện có. | `src/task9_retrieval_pipeline.py` | Done |
| Edge tests | Bổ sung test cho RRF rỗng, PageIndex thiếu API key và dense rỗng. | `tests/test_edge_cases.py` | Done |
| Threshold calibration | Chuẩn bị khả năng đọc `SCORE_THRESHOLD` từ `.env`; chưa đo ngưỡng trên ChromaDB thật vì môi trường embedding chưa sẵn sàng. | `src/task9_retrieval_pipeline.py` | Partial |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Gộp kết quả bằng Reciprocal Rank Fusion thay vì cộng trực tiếp điểm dense và BM25.  
   **Lý do/evidence:** Cosine similarity và BM25 sử dụng hai thang điểm khác nhau; RRF chỉ dựa trên vị trí xếp hạng nên tránh việc một thang điểm lấn át thang còn lại. .  
   **Trade-off:** RRF ổn định và đơn giản.

2. **Quyết định:** Chỉ dùng điểm dense gốc để kích hoạt PageIndex và luôn có đường lui về kết quả hybrid.  
   **Lý do/evidence:** Điểm RRF phản ánh thứ hạng, không phải độ tương đồng cosine nên không phù hợp để so với `SCORE_THRESHOLD`. Contract test xác nhận fallback được gọi khi dense score thấp và pipeline không crash khi PageIndex lỗi.  
   **Trade-off:** Khi dense đánh giá thấp nhưng BM25 tìm đúng từ khóa, hệ thống vẫn thử PageIndex và làm tăng độ trễ.

## Kiểm thử và kết quả

- Test đã chạy:
  - `python -m src.task7_reranking`
  - `python -m pytest tests/test_contracts.py -k "rrf or retrieve or pageindex" -q`
  - `python -m pytest tests/test_edge_cases.py -k "rerank_rrf or pageindex or empty_dense" -q`
- Kết quả: Đợi kết quả  của thành viên khác , chạy  test 4 contract test và 3 edge test của Track C đều pass.
- Lỗi đã phát hiện và cách xử lý:
  - Trùng ID giữa dense và BM25: cộng điểm theo thứ hạng nhưng chỉ trả một kết quả.
  - Dense rỗng: dùng điểm mặc định `0.0`, thử fallback và không truy cập `dense[0]`.
  - Thiếu `PAGEINDEX_API_KEY` hoặc dịch vụ lỗi: trả `[]`/hybrid thay vì làm dừng pipeline.

## Điều còn hạn chế

- Làm việc trong thời gian gắn , đợi modul của đồng đội khiến chưa test được modul thực tế 
- 
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện là hoàn thành index BGE-M3, đo sáu truy vấn hiệu chỉnh và ghi bằng chứng vào `reports/RESULT.md`.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 26/09/2026
- Tên thành viên: Phùng Trọng Chiến
