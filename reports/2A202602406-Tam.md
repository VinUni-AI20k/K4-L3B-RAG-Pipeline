# Individual contribution report

---

## Thông tin

- Họ và tên: Lê Công Tâm
- Mã học viên: 2A202602406
- Nhóm: K4-L3B (Nhóm Day 8 RAG Pipeline)
- Repository/branch: `origin/data` (hoặc nhánh làm việc cá nhân của Data Engineer)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Phân công nhóm | Khởi tạo file phân công các thành viên và vai trò | `TEAMMATES.md` | Done |
| Task 1 — Legal docs | Thu thập & xác thực 3 tài liệu quy chế ĐHQGHN (.pdf > 1KB) | `src/task1_collect_legal_docs.py`, `data/landing/legal/` | Done |
| Task 2 — Crawl news | Crawl 6 bài thông báo UET bằng Crawl4AI, lưu JSON kèm metadata | `src/task2_crawl_news.py`, `data/landing/news/` | Done |
| Task 3 — Chuẩn hóa Markdown | Chuyển đổi PDF bằng MarkItDown và format tin tức JSON sang Markdown | `src/task3_convert_markdown.py`, `data/standardized/` | Done |
| Acceptance Test | Chạy kiểm thử chấp nhận cho bộ dữ liệu corpus | `tests/test_acceptance.py` (3 test data) | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Sử dụng cơ chế kiểm tra cache dữ liệu (bỏ qua nếu file hợp lệ đã tồn tại) trong `task2_crawl_news.py` và `task3_convert_markdown.py`.  
   **Lý do/evidence:** Quá trình crawl bằng Crawl4AI / Playwright trên trình duyệt mất khoảng 30–35 giây cho mỗi trang web. Nếu mỗi lần chạy pipeline đều crawl lại từ đầu sẽ mất hơn 3 phút và dễ bị WAF/rate-limit.  
   **Trade-off:** Cần thêm tham số `force=True` khi muốn cập nhật lại toàn bộ nội dung mới từ web.

2. **Quyết định:** Chuẩn hóa cấu trúc Markdown phân tầng rõ ràng giữa `legal/` và `news/`, đồng thời gắn metadata (Source, Title, Date/Type) vào header mỗi file.  
   **Lý do/evidence:** Giúp module Chunking & Indexing (Task 4) dễ dàng trích xuất metadata để phục vụ Citation và lọc theo loại văn bản trong quá trình truy xuất (Retrieval).  
   **Trade-off:** File Markdown tăng nhẹ dung lượng do có thêm block header metadata.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
  ```bash
  pytest tests/test_acceptance.py -v -k "not test_golden and not test_eval"
  ```
- Kết quả trước/sau nếu có:
  - **Trước:** Chạy `task1`, `task2`, `task3` bị lỗi `NotImplementedError`, thiếu cấu trúc kiểm tra dữ liệu.
  - **Sau:** 3/3 bài test acceptance của Data PASSED 100%:
    + `test_corpus_has_required_legal_documents`: PASSED (3/3 file PDF quy chế).
    + `test_corpus_has_required_news_with_metadata`: PASSED (6/6 JSON có đủ `url`, `title`, `date_crawled`, `content_markdown`).
    + `test_standardized_output_covers_both_source_types`: PASSED (tất cả các file Markdown đều >= 200 ký tự).
- Lỗi đã phát hiện và cách xử lý:
  - Gặp lỗi mã hóa ký tự `UnicodeEncodeError (charmap / cp1252)` trên terminal Windows khi `print()` chuỗi tiếng Việt.
  - Cách xử lý: Chuẩn hóa thông điệp logging console sang dạng định dạng rõ ràng, tránh ký tự Unicode không tương thích mà vẫn đảm bảo log dễ hiểu.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Tốc độ trích xuất nội dung từ các file PDF dung lượng lớn (như quy trình cảnh báo học vụ > 1.2MB) qua công cụ convert còn phụ thuộc vào CPU.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung module tiền xử lý làm sạch văn bản chuyên sâu (loại bỏ số trang lặp lại, header/footer thừa trong file PDF) để chunk đầu ra của nhóm ML Engineer đạt độ chính xác cao hơn nữa.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Lê Công Tâm
