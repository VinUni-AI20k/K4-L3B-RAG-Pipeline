# Individual contribution report

---

## Thông tin

- **Họ và tên:** Vũ Văn Điền
- **Mã học viên:** 2A202602418
- **Nhóm:** Uống nước đẹp da
- **Repository/branch:** `VuVanDien-2A202602418`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 8 — PageIndex fallback | Implement `pageindex_search()` với graceful fallback khi không có API key, cache document IDs | `src/task8_pageindex_vectorless.py` | Done |
| Task 9 — Retrieval pipeline | Implement `retrieve()`: chạy dense + BM25, RRF fuse một lần, fallback dùng cosine score gốc | `src/task9_retrieval_pipeline.py` | Done |
| Task 10 — Generation & citation | Implement `reorder_for_llm()`, `format_context()`, `call_llm()` (Anthropic/OpenAI/Gemini), `generate_with_citation()` với safe refusal | `src/task10_generation.py` | Done |
| Streamlit UI | Xây dựng giao diện chat dark-theme, hiển thị sources với score bar, retrieval method badge, session stats | `app.py` | Done |
| Debug & fix pipeline | Phát hiện lỗi embedding mismatch (OpenAI dim=1536 vs sentence_transformers), lỗi proxy model, fix call_llm cho OpenAI-compatible proxy | `debug_pipeline.py`, `.env` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng `openai.OpenAI` client thay vì `anthropic.Anthropic` SDK để gọi LLM.  
   **Lý do/evidence:** Proxy `mwapi.dev` dùng OpenAI-compatible format (`/v1/chat/completions`), không phải Anthropic native endpoint (`/v1/messages`). Test bằng `requests` xác nhận status 200 với format OpenAI.  
   **Trade-off:** Mất tính năng native của Anthropic SDK (streaming token, tool use), nhưng đảm bảo tương thích với proxy và dễ switch provider.

2. **Quyết định:** Nhúng system prompt vào user message thay vì dùng `system` role riêng.  
   **Lý do/evidence:** Model `claude-haiku-4-5-20251001` trên proxy bị lock identity (từ chối làm chatbot khác), chuyển sang `claude-sonnet-4-6` và merge instruction vào user message để model tiếp nhận đúng nhiệm vụ.  
   **Trade-off:** Context dài hơn một chút, nhưng hoạt động ổn định và nhất quán trên mọi model của proxy.

## Kiểm thử và kết quả

- **Test query in-domain:** "Hoàn tiền mất bao lâu để nhận được?" → Hybrid retrieval 5 chunks, latency ~2s, answer có citation [Document N] đúng format.
- **Test pipeline debug:** Chạy `debug_pipeline.py` để trace từng bước: semantic search (dense), BM25, RRF fusion, generation — phát hiện `EMBEDDING_PROVIDER` mismatch giữa `.env` và ChromaDB thực tế (dim=1536 = OpenAI).
- **Kết quả trước/sau fix:**
  - Trước: trả `SAFE_REFUSAL` do model Kiro từ chối + embedding provider sai
  - Sau: trả câu trả lời tiếng Việt đúng ngữ cảnh, có citation, từ model `claude-sonnet-4-6`
- **Lỗi đã phát hiện:** `No module 'sentence_transformers'` → fix bằng cách align EMBEDDING_PROVIDER với ChromaDB thực tế.

## Điều còn hạn chế

- **Hạn chế:** RRF score rất nhỏ (0.016–0.033) do chỉ rank-based, không phản ánh relevance thực sự; threshold calibration cho fallback chưa được đo trên tập in/out-of-domain đủ lớn.
- **Nếu có thêm thời gian:** Calibrate `SCORE_THRESHOLD` bằng cách chạy ≥20 query in-domain và out-of-domain, vẽ precision-recall curve để chọn threshold tối ưu; thêm streaming response vào UI.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- **Ngày:** 25/09/2026
- **Tên thành viên:** Vũ Văn Điền
