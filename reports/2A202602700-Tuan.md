# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Nguyễn Anh Tuấn
- Mã học viên: 2A202602700
- Nhóm: Enigma
- Repository/branch: https://github.com/VinUni-AI20k/K4-L3B-RAG-Pipeline

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Streamlit UI | Xây dựng giao diện IELTS Writing RAG Lab gồm trang theo dõi quy trình Agent, chat demo, lịch sử hội thoại, câu hỏi gợi ý, source card và trạng thái loading/lỗi; hoàn thiện responsive, accessibility và độ tương phản của chat/input. | `app.py`; commits `02f8cbb`, `f6f558c`, `0a29581` | Done |
| UI–RAG integration | Tạo lớp hỗ trợ đọc trạng thái artifact, nối UI với contract `generate_with_citation()`, kiểm tra `GenerationResult` và giữ giao diện không bị crash khi pipeline/provider chưa sẵn sàng. | `src/ui_support.py`; commit `02f8cbb` | Done |
| Tài liệu và điều phối nhóm | Ghi nhận thành viên/vai trò và xây dựng checklist yêu cầu, rubric, thứ tự triển khai, Definition of Done để nhóm theo dõi tiến độ. | `TEAMMATES.md`, `docs/PROJECT_REQUIREMENTS_CHECKLIST.md`; commit `02f8cbb` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Tách logic kết nối pipeline khỏi giao diện qua `src/ui_support.py`, để `app.py` chỉ phụ thuộc vào contract công khai `generate_with_citation()`.  
   **Lý do/evidence:** `run_rag_query()` dùng lazy import, gọi `validate_generation_result()` và chuyển `NotImplementedError`/lỗi provider thành thông báo an toàn; vì vậy UI vẫn khởi động trong khi các module RAG đang được thành viên khác hoàn thiện.  
   **Trade-off:** Cách này tăng thêm một lớp trung gian và hiện chỉ hiển thị safe fallback, chưa tạo được câu trả lời thật khi Task 10 còn `NotImplementedError`.

2. **Quyết định:** Hiển thị trạng thái pipeline từ artifact thực tế theo hướng bảo thủ, không coi placeholder là đã hoàn thành.  
   **Lý do/evidence:** `get_project_snapshot()` đếm dữ liệu/golden cases và `_implemented()` kiểm tra `TODO:` hoặc `NotImplementedError`; dashboard đồng thời trình bày input, action, output, phương pháp và cấu hình của từng bước.  
   **Trade-off:** Kiểm tra tĩnh có thể chưa phản ánh đầy đủ chất lượng runtime; một module hết placeholder vẫn cần test tích hợp để xác nhận hoạt động đúng.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `python -m py_compile app.py src/ui_support.py`; gọi `get_project_snapshot()`; gọi `run_rag_query("Band 7 vs Band 6?", 5)` khi Task 10 chưa triển khai.
- Kết quả trước/sau nếu có: Hai file UI compile thành công; snapshot đọc đúng trạng thái hiện có; query trả `retrieval_source="none"` cùng thông báo pipeline chưa được nối thay vì làm ứng dụng crash.
- Lỗi đã phát hiện và cách xử lý: Sidebar mặc định và nút câu hỏi dài chưa phù hợp, chữ trong chat/input thiếu tương phản; đã đổi sidebar sang `auto`, cho phép nút tự giãn/wrap và bổ sung style cho chat, alert, placeholder và nút gửi trong commits `f6f558c`, `0a29581`.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: UI đã hoàn thiện nhưng chưa thể kiểm chứng luồng RAG end-to-end vì `src/task10_generation.py` vẫn còn placeholder và `NotImplementedError`.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung test tự động cho `ui_support.py` (snapshot, contract validation, fallback lỗi) và chạy smoke test Streamlit với pipeline hoàn chỉnh.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Nguyễn Anh Tuấn
