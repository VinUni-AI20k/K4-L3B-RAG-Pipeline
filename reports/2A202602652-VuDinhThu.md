# Individual Contribution Report

## Thông tin

- Họ và tên: Vũ Đình Thư
- Mã học viên: 2A202602652
- Nhóm: K4-L3B-RAG-Pipeline
- Vai trò: Member & UI / Evaluation Engineer

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Chatbot UI | Phát triển giao diện người dùng tương tác bằng Streamlit, hiển thị câu trả lời, nguồn trích dẫn, score và phương thức truy xuất | `app.py` | Done |
| Xây dựng Golden Dataset | Soạn thảo bộ dữ liệu đánh giá 18 câu hỏi Q&A chuẩn (bao gồm câu hỏi luật, danh lam thắng cảnh, ẩm thực và câu ngoài phạm vi) | `group_project/evaluation/golden_dataset.json` | Done |
| Đánh giá A/B & Metrics | Thực nghiệm đo lường 4 chỉ số (Faithfulness, Relevance, Recall, Precision) so sánh Dense-only và Hybrid RRF | `reports/RESULT.md`, `group_project/evaluation/RESULT.md` | Done |
| Phân tích lỗi & Báo cáo | Tổng hợp các trường hợp truy vấn kém (worst performers), phân tích nguyên nhân gốc rễ và đề xuất cải tiến | `reports/RESULT.md` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Thiết kế UI hiển thị nguồn tham khảo bên trong `st.expander` kèm metadata chi tiết (title, file nguồn, score, phương pháp truy xuất).  
   **Lý do/evidence:** Giúp giao diện trò chuyện gọn gàng, người dùng bình thường đọc câu trả lời trực tiếp mà không bị rối mắt, nhưng chuyên gia/giảng viên vẫn có thể mở ra kiểm chứng bằng chứng nguồn gốc.  
   **Trade-off:** Cần lưu trạng thái lịch sử hội thoại có cấu trúc lồng nhau trong `st.session_state`.

2. **Quyết định:** Bổ sung slider cho phép người dùng tùy chỉnh `top_k` (từ 3 đến 10 chunks) trên thanh Sidebar.  
   **Lý do/evidence:** Cho phép thử nghiệm trực tiếp mức độ ảnh hưởng của số lượng ngữ cảnh đưa vào LLM đối với chất lượng sinh câu trả lời trong lúc demo.  
   **Trade-off:** Giá trị mặc định vẫn giữ là 5 để đảm bảo hiệu năng và tính ổn định.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy `streamlit run app.py` và kiểm thử tương tác với 10 câu hỏi ngẫu nhiên trong golden dataset.
- Kết quả trước/sau: Giao diện hiển thị mượt mà, phản hồi có trích dẫn rõ ràng, hiển thị đúng nhãn `hybrid` hoặc `pageindex`.
- Lỗi đã phát hiện và cách xử lý: Lỗi không cập nhật lại tin nhắn khi người dùng nhập câu hỏi mới; đã cấu hình lưu trữ tin nhắn vào session_state đầy đủ cả role `user` và `assistant`.

## Điều còn hạn chế

- Một hạn chế cụ thể: Giao diện hiện tại chưa có tính năng ghi nhớ ngữ cảnh đa lượt (conversation multi-turn memory) mà chỉ xử lý từng lượt hỏi đáp độc lập.
- Hướng cải tiến: Bổ sung LangChain `ConversationBufferMemory` hoặc Chat history tóm tắt để hỗ trợ các câu hỏi tiếp nối (follow-up questions).

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Vũ Đình Thư
